# Project specs

Each feature folder contains its project specification and supporting documents.

Captured source material that is not yet normalized into a feature spec lives in
`.agents/specs/intake/`. Do not change a spec's status or content solely because
it is old; review status and content as separate assigned work.
