[package]
name = "daw-core"
version = "0.1.0"
edition = "2021"
description = "Core Domain Types (TrackId, Beats) for Sourdaw, used everywhere."

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
