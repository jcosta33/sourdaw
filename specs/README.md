# Project specs

This repository keeps checked-in feature specs as an explicit project-governance
exception. Each feature folder may contain its spec and supporting documents.

Captured source material that is not yet normalized into a feature spec lives in
`specs/intake/`. Do not change a spec's status or content solely because it is
old; review status and content as separate assigned work.
