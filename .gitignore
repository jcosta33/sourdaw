.DS_Store
node_modules
dist
dist-ssr
coverage
*.local
.env
.env.*
!.env.example

# Tauri
target
src-tauri/target/

# Editor
.vscode/
!.vscode/extensions.json
*.sw?

# Agent task files are worktree-specific and not shared
.agents/tasks/*
.agents/prompts/*
!.agents/tasks/.gitkeep

# Knip output (regenerated per session; dead-code audit lives in ../sourdaw-hq/audits/deadcode.md)
knip-results.txt

# Playwright
playwright-report/
test-results/

# Agent UI Scripts
.agents/ui-scripts/*.png

# Swarm — agent scratch and future-CLI state (workspace lives in ../sourdaw-hq)
.agents/tmp/
tmp/
.swarm/
