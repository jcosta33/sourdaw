# CODEOWNERS
#
# One owner for the whole repository. Every pull request auto-requests review from them.
#
# Only a user, a team, or an account email may own a path, and the owner must hold write access.
# `jcosta33-reviewer[bot]` is a GitHub App and therefore cannot be listed here. The reviewer bot's
# approval is enforced instead by `pnpm deliver`, which refuses to merge a pull request the bot has
# not APPROVED on the current head. That gate is the binding one; this file only routes the request.
#
# Do not reintroduce a per-module owner map. `jcosta33/sourdaw` is a personal repository, so the
# `@sourdaw/team-*` organisation teams a previous revision of this file addressed cannot exist, and
# every one of its lines resolved to nobody.
#
# See: https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners

*  @jcosta33
