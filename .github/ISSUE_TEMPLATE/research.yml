name: Research
description: Evidence for a decision. Advisory only — do not write requirements here.
title: "research(scope): "
labels: ["status:tracking"]
body:
  - type: markdown
    attributes:
      value: |
        One question. Fit evidence. Intent and requirements stay on a Spec.

        Mark unreachable claims [unconfirmed]. Unpublished work stays in `~/.agents/artifacts`. Do not submit empty R-001 shells.

  - type: dropdown
    id: scope
    attributes:
      label: Subsystem
      options:
        - audio-engine
        - daw-dsp
        - electron
        - native
        - plugin-host
        - midi
        - arrangement
        - transport
        - yeast / bacteria / knead / levain / crumbs / grinder / grand-boule / fermenter
        - ai
        - workspace-shell / ui
        - security
        - build
        - cross-cutting
    validations:
      required: true

  - type: textarea
    id: question
    attributes:
      label: Question and scope
      description: The decision, audience, time horizon, and reversal evidence.
    validations:
      required: true

  - type: textarea
    id: method
    attributes:
      label: Method and coverage
      description: What was searched, exercised, or excluded.
    validations:
      required: true

  - type: textarea
    id: findings
    attributes:
      label: Findings
      description: One claim per R-NNN. Separate observation, source claim, and inference.
      placeholder: |
        ### R-001
        - Claim:
        - Evidence:
        - Confidence: High | Moderate | Low | Very low
        - Decision relevance:
    validations:
      required: true

  - type: textarea
    id: options
    attributes:
      label: Options comparison
      description: Named criteria. Preserve disconfirming evidence.
      placeholder: |
        | Option |  |  |
        | --- | --- | --- |
        |  |  |  |
    validations:
      required: true

  - type: textarea
    id: open_questions
    attributes:
      label: Open questions
      description: Exact question or test that unlocks direction. Use None if the evidence already chooses.
      placeholder: "None"
    validations:
      required: true

  - type: textarea
    id: recommendation
    attributes:
      label: Advisory recommendation
      description: Tie the recommendation to finding IDs. Do not author Intent or AC-NNN here.
    validations:
      required: true
