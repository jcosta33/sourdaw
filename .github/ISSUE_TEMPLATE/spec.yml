name: Spec
description: Decided intent that must become acceptance criteria before implementation. Not for trivial work or drafts.
title: "spec(scope): "
labels: ["status:tracking"]
body:
  - type: markdown
    attributes:
      value: |
        File only when every blocking choice is resolved and the content can be public. Unpublished or secret work stays in `~/.agents/artifacts` and is not filed.

        The issue body is the spec. Delete any local copy after filing. Implementation slices use **Task**. After create, add `priority:P*` to match the dropdown; this form cannot apply it.

  - type: dropdown
    id: scope
    attributes:
      label: Subsystem
      options:
        - audio-engine
        - daw-dsp
        - electron
        - native
        - plugin-host
        - midi
        - arrangement
        - transport
        - yeast / bacteria / knead / levain / crumbs / grinder / grand-boule / fermenter
        - ai
        - workspace-shell / ui
        - security
        - build
        - cross-cutting
    validations:
      required: true

  - type: dropdown
    id: priority
    attributes:
      label: Priority
      options:
        - P0
        - P1
        - P2
        - P3
    validations:
      required: true

  - type: textarea
    id: intent
    attributes:
      label: Intent
      description: What must become true, for whom, and where it stops. No implementation plan.
    validations:
      required: true

  - type: textarea
    id: requirements
    attributes:
      label: Requirements
      description: One independently failing obligation per AC. Exactly those three lines, in that order. One MUST / MUST NOT / SHOULD / SHOULD NOT / MAY in Then. Empty shells are not a spec.
      placeholder: |
        ### AC-001
        - When:
        - Then:
        - Verify with:
    validations:
      required: true

  - type: textarea
    id: boundary
    attributes:
      label: Boundary
      description: Optional. Add only if the intent does not already say what is out of scope.
    validations:
      required: false

  - type: textarea
    id: sources
    attributes:
      label: Sources
      description: ADRs, parent campaign, research issue, or other GitHub URLs. No local file paths.
      placeholder: "ADR 0021, #2037"
    validations:
      required: false
