# Dead Code Inventory

Generated by `npx knip --no-exit-code`. 59 unused files identified.

## Categories

### Empty barrel files (28) — safe to remove
All are `events/index.ts`, `useCases/index.ts`, `stores/index.ts`, `models/index.ts`
with no re-exports. Created for contract-folder pattern but never populated.

### Dead implementations (17) — review before removing
- `LlmOrchestrationTypes.ts` — unused types
- `nativeEngine/lifecycle.ts`, `webLlm/engineLifecycle.ts` — unused engine lifecycle
- `handlers/types.ts` — unused handler types
- `audioAnalysis/index.ts` — empty barrel
- `bakeScaleFolding.ts` — unused clip operation
- `swipeGroupComp.ts` — unused comp operation (has stub test)
- `AudioGraph.ts`, `SidechainRoute.ts` — unused models
- 5× `webMidi/*` — unused Web MIDI lifecycle code
- `controllerScriptingWorker.ts` — unused worker
- 3× `pluginBridge/*` — unused plugin bridge functions
- 3× `nativeProjectFiles/*` — unused native file I/O
- `projectTemplates/index.ts` — empty barrel

### UI components (3) — verify before removing
- `BounceOptionsDialog.tsx` — unreferenced dialog
- `WebGpuSpectrumAnalyzer.tsx` — unreferenced WebGPU renderer
- `createWebGpuSpectrumRenderer.ts` — unreferenced renderer factory

### Misc (2)
- `Inspector/layouts/index.ts` — empty barrel
- `setFillActive.ts` — unused Toaster use case

## Action required
These files need explicit human approval to delete (per repo safety rules).
Each contributes 0% to coverage since they're never imported.
