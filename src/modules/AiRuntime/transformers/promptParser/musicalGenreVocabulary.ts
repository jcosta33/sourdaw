/**
 * Genre and style words a request uses instead of naming objects. Shared so complexity routing and
 * plan-created object evidence read one vocabulary: two copies drift, and the drift is silent.
 */
export const MUSICAL_GENRE_PATTERN =
    /\b(blues|jazz|rock|pop|hip.?hop|trap|edm|classical|folk|country|r&b|rnb|funk|reggae|metal|punk|ambient|lo.?fi)\b/i;
