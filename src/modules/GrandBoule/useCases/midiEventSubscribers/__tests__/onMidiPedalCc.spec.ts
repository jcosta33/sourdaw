import { describe, it, expect } from 'vitest';

import * as subject from '../onMidiPedalCc';

describe('onMidiPedalCc', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
