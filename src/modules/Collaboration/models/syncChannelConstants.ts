/** Pseudo-docId used to route asset-transfer messages over the CRDT sync channel. */
export const DOC_ID_ASSET = '__asset__' as const;

/** Pseudo-docId used to route branch-metadata messages over the CRDT sync channel. */
export const DOC_ID_BRANCHES = '__branches__' as const;
