import { describe, it, expect } from 'vitest';

import * as subject from '../addToRecentProjects';

describe('addToRecentProjects', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
