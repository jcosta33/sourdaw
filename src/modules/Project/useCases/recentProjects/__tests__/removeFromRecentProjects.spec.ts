import { describe, it, expect } from 'vitest';

import * as subject from '../removeFromRecentProjects';

describe('removeFromRecentProjects', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
