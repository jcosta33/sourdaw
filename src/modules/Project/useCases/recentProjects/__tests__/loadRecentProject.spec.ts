import { describe, it, expect } from 'vitest';

import * as subject from '../loadRecentProject';

describe('loadRecentProject', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
