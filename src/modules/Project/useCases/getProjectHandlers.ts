// Failed migration stub retained because this repository forbids file deletion without explicit instruction.
// Project persistence handlers remain in Workspace until Project's persistence graph is split enough
// to avoid worsening dep-cruiser cycles.
