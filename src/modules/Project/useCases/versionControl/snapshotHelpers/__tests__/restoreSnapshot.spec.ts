import { describe, it, expect } from 'vitest';

import * as subject from '../restoreSnapshot';

describe('restoreSnapshot', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
