/**
 * useGlobalKeyboardShortcuts — local re-export for Workspace module.
 * Imports from Command's view contract (presentations/views/ is an
 * allowed cross-module import target).
 */
export { useGlobalKeyboardShortcuts } from '#/modules/Command/presentations/views/keyboardShortcutsContract';
