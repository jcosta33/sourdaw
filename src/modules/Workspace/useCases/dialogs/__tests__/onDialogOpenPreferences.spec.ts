import { describe, it, expect } from 'vitest';

import * as subject from '../onDialogOpenPreferences';

describe('onDialogOpenPreferences', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
