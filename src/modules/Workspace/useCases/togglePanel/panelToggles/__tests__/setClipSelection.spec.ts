import { describe, it, expect } from 'vitest';
import { setClipSelection } from '../setClipSelection';
describe('setClipSelection', () => {
    it('is a function', () => { expect(typeof setClipSelection).toBe('function'); });
});
