import { describe, it, expect } from 'vitest';

import * as subject from '../onZoomToSelection';

describe('onZoomToSelection', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
