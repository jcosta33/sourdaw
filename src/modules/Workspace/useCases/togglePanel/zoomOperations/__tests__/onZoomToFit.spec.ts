import { describe, it, expect } from 'vitest';
import * as subject from '../onZoomToFit';

describe('onZoomToFit', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
