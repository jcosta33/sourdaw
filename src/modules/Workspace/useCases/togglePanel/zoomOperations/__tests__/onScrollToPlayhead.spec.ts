import { describe, it, expect } from 'vitest';

import * as subject from '../onScrollToPlayhead';

describe('onScrollToPlayhead', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
