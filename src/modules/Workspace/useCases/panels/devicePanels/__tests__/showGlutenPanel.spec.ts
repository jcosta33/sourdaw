import { describe, it, expect } from 'vitest';

import * as subject from '../showGlutenPanel';

describe('showGlutenPanel', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
