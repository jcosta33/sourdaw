import { describe, it, expect } from 'vitest';
import * as subject from '../showFermenterPanel';

describe('showFermenterPanel', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
