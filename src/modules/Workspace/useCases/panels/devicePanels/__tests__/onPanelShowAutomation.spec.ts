import { describe, it, expect } from 'vitest';

import * as subject from '../onPanelShowAutomation';

describe('onPanelShowAutomation', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
