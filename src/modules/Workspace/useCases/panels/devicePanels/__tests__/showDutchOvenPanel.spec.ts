import { describe, it, expect } from 'vitest';
import * as subject from '../showDutchOvenPanel';

describe('showDutchOvenPanel', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
