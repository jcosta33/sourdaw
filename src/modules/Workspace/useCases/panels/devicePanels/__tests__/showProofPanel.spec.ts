import { describe, it, expect } from 'vitest';
import * as subject from '../showProofPanel';

describe('showProofPanel', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
