import { describe, it, expect } from 'vitest';
import * as subject from '../onPanelShowGrinder';

describe('onPanelShowGrinder', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
