import { describe, it, expect } from 'vitest';

import * as subject from '../onPanelShowBacteria';

describe('onPanelShowBacteria', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
