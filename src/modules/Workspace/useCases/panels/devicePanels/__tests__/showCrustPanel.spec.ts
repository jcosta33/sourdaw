import { describe, it, expect } from 'vitest';

import * as subject from '../showCrustPanel';

describe('showCrustPanel', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
