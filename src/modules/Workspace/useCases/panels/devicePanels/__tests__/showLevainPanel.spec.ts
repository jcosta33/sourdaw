import { describe, it, expect } from 'vitest';
import * as subject from '../showLevainPanel';

describe('showLevainPanel', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
