import { describe, it, expect } from 'vitest';
import * as subject from '../onPanelShowFermenter';

describe('onPanelShowFermenter', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
