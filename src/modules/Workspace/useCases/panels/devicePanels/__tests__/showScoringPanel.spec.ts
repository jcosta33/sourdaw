import { describe, it, expect } from 'vitest';

import * as subject from '../showScoringPanel';

describe('showScoringPanel', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
