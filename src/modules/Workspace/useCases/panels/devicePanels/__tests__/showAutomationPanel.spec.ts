import { describe, it, expect } from 'vitest';
import * as subject from '../showAutomationPanel';

describe('showAutomationPanel', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
