import { describe, it, expect } from 'vitest';

import * as subject from '../showToasterPanel';

describe('showToasterPanel', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
