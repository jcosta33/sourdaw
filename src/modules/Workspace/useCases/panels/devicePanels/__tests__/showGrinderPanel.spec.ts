import { describe, it, expect } from 'vitest';
import * as subject from '../showGrinderPanel';

describe('showGrinderPanel', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
