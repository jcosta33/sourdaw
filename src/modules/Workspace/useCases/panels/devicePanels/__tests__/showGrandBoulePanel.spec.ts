import { describe, it, expect } from 'vitest';
import * as subject from '../showGrandBoulePanel';

describe('showGrandBoulePanel', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
