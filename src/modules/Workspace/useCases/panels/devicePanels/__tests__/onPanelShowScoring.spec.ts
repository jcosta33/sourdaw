import { describe, it, expect } from 'vitest';

import * as subject from '../onPanelShowScoring';

describe('onPanelShowScoring', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
