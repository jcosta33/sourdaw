import { describe, it, expect } from 'vitest';
import * as subject from '../onPanelShowGrandBoule';

describe('onPanelShowGrandBoule', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
