import { describe, it, expect } from 'vitest';

import * as subject from '../onPanelShowProof';

describe('onPanelShowProof', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
