import { describe, it, expect } from 'vitest';

import * as subject from '../onPanelShowYeast';

describe('onPanelShowYeast', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
