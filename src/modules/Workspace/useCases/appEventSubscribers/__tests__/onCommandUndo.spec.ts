import { describe, it, expect } from 'vitest';
import * as subject from '../onCommandUndo';

describe('onCommandUndo', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
