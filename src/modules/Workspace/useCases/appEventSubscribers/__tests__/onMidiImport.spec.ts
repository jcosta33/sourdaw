import { describe, it, expect } from 'vitest';

import * as subject from '../onMidiImport';

describe('onMidiImport', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
