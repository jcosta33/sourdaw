import { describe, it, expect } from 'vitest';
import * as subject from '../pluginQueries';

describe('pluginQueries', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
