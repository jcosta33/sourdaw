import { describe, it, expect } from 'vitest';
import * as subject from '../levainBridge';

describe('levainBridge', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
