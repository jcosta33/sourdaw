import { describe, it, expect } from 'vitest';
import * as subject from '../setBacteriaParamWithAudio';

describe('setBacteriaParamWithAudio', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
