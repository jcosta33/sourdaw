/** Shared type used by all device node factories. Defined in models to keep it importable by engine/services without crossing the repository boundary. */
export type { OfflineDeviceNode } from '../../models/OfflineDeviceNode';
