import { describe, it, expect } from 'vitest';
import * as subject from '../stopMidiLearn';

describe('stopMidiLearn', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
