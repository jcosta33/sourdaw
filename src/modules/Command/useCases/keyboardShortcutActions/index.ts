// Transport
export { togglePlayback, stopPlayback, toggleLoop, toggleMetronome, toggleRecording, seekPlayhead } from './transportShortcuts';

// Track / Clip
export { clearSolos, addTrack, duplicateTrack, duplicateClip, duplicateClipToNextBar, zoomTracksVertical } from './trackShortcuts';

// Workspace
export { setEditingTool, zoomToFit, zoomToSelection } from './workspaceShortcuts';
