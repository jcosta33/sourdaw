import { describe, it, expect } from 'vitest';

import * as subject from '../handleKeydown';

describe('handleKeydown', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
