import { describe, it, expect } from 'vitest';
import * as subject from '../handleKeyup';

describe('handleKeyup', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
