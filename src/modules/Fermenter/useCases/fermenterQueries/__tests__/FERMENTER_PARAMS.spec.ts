import { describe, it, expect } from 'vitest';

import * as subject from '../FERMENTER_PARAMS';

describe('FERMENTER_PARAMS', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
