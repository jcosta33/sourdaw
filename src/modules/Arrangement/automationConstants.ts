/**
 * Shared constants for inline automation sub-lanes in the arrangement view.
 */

/** Height in pixels of each inline automation sub-lane below a track */
export const AUTOMATION_SUB_LANE_HEIGHT = 40;
