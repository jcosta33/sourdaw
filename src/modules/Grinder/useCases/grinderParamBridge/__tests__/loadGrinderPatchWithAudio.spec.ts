import { describe, it, expect } from 'vitest';
import * as subject from '../loadGrinderPatchWithAudio';

describe('loadGrinderPatchWithAudio', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
