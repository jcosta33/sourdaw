import { describe, it, expect } from 'vitest';
import * as subject from '../setGrinderParamWithAudio';

describe('setGrinderParamWithAudio', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
