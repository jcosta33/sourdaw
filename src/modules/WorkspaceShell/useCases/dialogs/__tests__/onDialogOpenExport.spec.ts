import { describe, it, expect } from 'vitest';

import * as subject from '../onDialogOpenExport';

describe('onDialogOpenExport', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
