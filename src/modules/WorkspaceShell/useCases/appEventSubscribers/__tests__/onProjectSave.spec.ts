import { describe, it, expect } from 'vitest';

import * as subject from '../onProjectSave';

describe('onProjectSave', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
