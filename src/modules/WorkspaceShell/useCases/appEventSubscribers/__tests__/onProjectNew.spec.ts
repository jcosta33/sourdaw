import { describe, it, expect } from 'vitest';

import * as subject from '../onProjectNew';

describe('onProjectNew', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
