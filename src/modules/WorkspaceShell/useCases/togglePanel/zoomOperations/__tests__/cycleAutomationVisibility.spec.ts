import { describe, it, expect } from 'vitest';

import * as subject from '../cycleAutomationVisibility';

describe('cycleAutomationVisibility', () => {
    it('should load the module', () => {
        expect(subject).toBeDefined();
    });
});
