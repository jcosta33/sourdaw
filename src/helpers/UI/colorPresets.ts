/**
 * @deprecated Shim — canonical location is `src/components/daw/colorPresets.ts`.
 * Preserved so existing module import paths continue to resolve during migration.
 */
export { TRACK_COLOR_PRESETS, CLIP_COLOR_PRESETS } from '#/components/daw/colorPresets';
